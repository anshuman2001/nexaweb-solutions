import type { ReactNode } from 'react';

export const metadata = {
  title: 'AI Calling Agent — DigiAgentix',
  description: 'Replace 500 sales agents with 1 AI. Human-like calls in Hindi & English, 24/7, at ₹33/call.',
};

export default function AICallingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
