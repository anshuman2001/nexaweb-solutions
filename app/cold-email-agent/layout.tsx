export const metadata = {
  title: 'Cold Email AI Agent — DigiAgentix',
  description: 'AI-powered cold email outreach for USA businesses',
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
